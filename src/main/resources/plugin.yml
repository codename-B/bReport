name: bReport
version: 1.5.7
author: [codename_B, ProjectBarks]
website: http://tetragaming.com/codename_B/
main: de.bananaco.report.ReportPlugin
commands:
    report:
        usage: /<command> message
    read:
        usage: /<command> id
    gotoreport:
        usage: /<command> id
    resolve:
        usage: /<command> id
    unresolve:
        usage: /<command> id
    modchat:
        aliases: [mc, mod]
        usage: /<command> message
    comment:
        usage: /<command> id [message]
        